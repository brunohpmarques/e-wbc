<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:id="@+id/btnBltDisabled"
        android:title="@string/disabled"
        app:showAsAction="always"/>
    <item android:id="@+id/btnBltActivated"
        android:visible="false"
        android:title="@string/activated"
        app:showAsAction="always"/>
    <item android:id="@+id/btnBltConnecting"
        android:visible="false"
        android:title="@string/connecting"
        app:showAsAction="always"/>
    <item android:id="@+id/btnBltConnected"
        android:visible="false"
        android:title="@string/connected"
        app:showAsAction="always"/>
</menu>